Squah merge:
Merges the branch and all changes will be ina single commit.

git checkout develop
git merge --squash feature/branch_name
git commit -m "Merged feature/branch_name"
git push origin develop

Reabse merge:
